import { Heading } from '@chakra-ui/react';

const ProductInfo = ({ FoodData }) => {
  console.log(FoodData);
  return <Heading>THIS IS SINGLE PRODUCT INFO SCREEN</Heading>;
};
export default ProductInfo;
